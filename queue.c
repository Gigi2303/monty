#include "monty.h"

/**
 * f_queue - this sets the format of the data structure to queue
 * @stack: pointer to the stack
 * @line_count: the line number in which the opcode is called
 */

void f_queue(stack_t **stack, unsigned int line_count)
{
	(void)stack;
	(void)line_count;
	statements->stack = 0;
}
